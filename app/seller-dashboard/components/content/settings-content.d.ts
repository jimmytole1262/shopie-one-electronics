import { ReactNode } from 'react';

interface SettingsContentProps {
  // Add any props needed for SettingsContent
}

declare function SettingsContent(props?: SettingsContentProps): ReactNode;

export default SettingsContent;
